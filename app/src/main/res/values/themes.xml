<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base de la Activity (la UI real se pinta con Compose vía CityGridTheme) -->
    <style name="Theme.CityGrid" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/background_light</item>
        <item name="android:statusBarColor">@color/citygrid_green_dark</item>
        <item name="android:navigationBarColor">@color/surface_card</item>
    </style>
</resources>
